<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8" />
	<title>Edit Cast</title>
</head>
<body>
	<form action="/casts/edit/<%= cast.id %>" method="POST">
		<input type="hidden" name="id" value="<%= cast.id %>"/>
		<input type="text" name="first_name" placeholder="first_name" value="<%= cast.first_name %>" />
		<input type="text" name="last_name" placeholder="last_name" value="<%= cast.last_name %>" />
		<input type="text" name="birth_year" placeholder="birth_year" value="<%= cast.birth_year %>" />
		<select name="gender" id="gender">
			<option <%= (cast.gender == "male") ? 'selected' : '' %> value="male">male</option>
			<option <%= (cast.gender == "female") ? 'selected' : '' %>  value="female">female</option>
		</select>

		<input type="submit" value="Tambah"/>
	</form>
</body>
</html>